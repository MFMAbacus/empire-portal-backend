// // refreshToken.js (or refreshToken.ts if you're using TypeScript)

// const NotificationFCM = require("@/utility/notification");

// (async () => {
//   try {
//     // Get the instance of NotificationFCM
//     const notificationFCM = NotificationFCM.getInstance();

//     // Refresh tokens and get the cached token
//     const token = await notificationFCM.refreshTokens();

//     // Output the token to the console
//     console.log("Token:", token);
//   } catch (error) {
//     console.error("Error fetching token:", error);
//   }
// })();
